# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# cp .env.example .env.local

# ===========================================
# Supabase Configuration (Required)
# ===========================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Public Supabase URL - safe to expose to browser
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public anonymous key - safe to expose to browser (has RLS protection)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key - KEEP SECRET! Used for admin operations that bypass RLS
# Only used server-side in API routes
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ===========================================
# AI Service Configuration (Required)
# ===========================================
# Google Gemini API Key for food image recognition
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# Optional Configuration
# ===========================================
# Uncomment and set these if needed

# Node environment (development | production | test)
# NODE_ENV=development

# Custom port for local development (default: 3000)
# PORT=3000

# ===========================================
# Deployment Notes
# ===========================================
# For Vercel: Add these in Project Settings → Environment Variables
# For Docker: Pass via --env-file .env.local or docker-compose.yml
# For Cloud Run: Set via --set-env-vars flag or Console UI
